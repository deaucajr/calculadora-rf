# _legado/ — versões antigas (referência histórica)

Código de etapas anteriores do projeto, **não usado** pelo fluxo atual. Mantido para
consulta. Pode ser removido se não for mais útil.

| Arquivo | Era |
|---|---|
| `daily_update.py` | rotina diária antiga (substituída por `scripts/rotina_diaria.py`) |
| `main.py` | CLI antigo de cálculo (substituído pelo add-in) |
| `test_calc.py` | teste do `calc.py` antigo (SQLite) |
| `DEB_Calc2.bas` | add-in anterior (substituído por `addin/RF_Calc.bas`) |
| `addin.py` | tentativa com xlwings (UDFs em Python) — exigia Python no Excel |
| `generate_dashboard.py` | gerador de dashboard `.xlsx` |
| `manual/` | cadastro manual de fluxos via planilha |
